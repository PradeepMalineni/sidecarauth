syntax = "proto3";

package token;

service TokenService {
  rpc BroadcastToken (TokenRequest) returns (stream TokenResponse);
}

message TokenRequest {
  string request_id = 1;
}

message TokenResponse {
  string token = 1;
}