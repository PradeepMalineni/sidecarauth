Fix Sonar issues in this Python code with safe, minimal refactors: keep public API and behavior identical (same inputs/outputs, logs, exceptions, data shapes), no new deps, ≤50 changed lines, helpers in same module. Return: 1) Plan, 2) Patch (unified diff), 3) Rationale, 4) Behavior Preservation Notes, 5) Guardrail check (Y/N).

You are a senior Python engineer. I want you to fix static analysis findings (SonarQube issues) in my Python code.

RULES:
- Do NOT change the public API (function/class names, parameters, or return types).
- Do NOT change behavior (I/O, logs, exceptions, data structures, SQL/HTTP strings).
- Do NOT add new runtime dependencies.
- Keep each patch small and focused (≤50 changed lines).
- If an issue cannot be fixed without altering behavior, explain why and suggest alternatives.
- Prefer safe refactoring: extract small pure helper functions with docstrings, use guard clauses to flatten nesting, replace repeated code with shared helpers, fix mutable defaults, and narrow exceptions.
- Keep helpers in the same module; no external imports unless standard library.

INPUT I WILL PROVIDE:
- Code (Python file)
- Sonar findings (rule, message, line numbers, thresholds if relevant)

OUTPUT FORMAT (always):
1. **Plan** — bullet points mapping each Sonar issue → planned fix.
2. **Patch** — unified diff with minimal changes for the provided file(s).
3. **Rationale** — how each change addresses a Sonar issue.
4. **Behavior Preservation Notes** — why behavior is unchanged.
5. **Follow-ups** — recommended tests to validate the fixes (names only).
6. **Guardrail Check** — Answer Y/N for:
   - Public API unchanged?
   - Inputs/outputs & side-effects identical?
   - Same exceptions raised?
   - No new deps added?
   - Changes limited to provided file(s)?
   - Patch ≤50 changed lines?

Please reduce cognitive complexity of <func_name> from <current> to <= <target>.
Preferred tactics: early returns, extracting small pure helpers (<20 lines) with docstrings, replacing nested if/elif with guard clauses or a dispatch map where equivalent.
Do not alter behavior or API.